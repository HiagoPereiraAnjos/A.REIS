import React from 'react';

// Compatibility component for stale builds that still reference `Lawyers`.
// Intentionally renders nothing because this section was removed.
const Lawyers: React.FC = () => null;

export default Lawyers;
